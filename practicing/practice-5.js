/**
 * 1. Napravi varijablu broj = 10.
 * Ako je broj veći od 5, ispiši "Broj je veći od 5".
 */

let number = 10;
if (number > 5) {
    console.log('Broj je veci od 5');
}

/**
 * 2. Napravi varijablu godine = 18.
 * Ako su godine jednake 18, ispiši "Punoletan".
 */

let year = 18;
if (year === 18) {
    console.log('Punoletan');
}

/**
 * 3. Napravi varijablu x = 3 i y = 5.
 * Ako je x manje od y, ispiši "x je manje od y".
 */

let x = 3;
let y = 5;

if (x < y) {
    console.log('x je manje od y');
}

/**
 * 4. Napravi varijablu poeni = 70.
 * Ako je poeni veće ili jednako 60, ispiši "Položio".
 */

let points = 70;
if (points >= 60) {
    console.log('Polozio');
}

/**
 * 5. Napravi varijablu temperatura = 30.
 * Ako je temperatura veća od 25, ispiši "Vruće je".
 */

let temperature = 30;
if (temperature > 25) {
    console.log('Vruce je');
}

/**
 * 6. Napravi varijablu jeOnline = true.
 * Ako je jeOnline true, ispiši "Korisnik je online".
 */

let isOnline = true;
if (isOnline) {
    console.log('Korisnik je online');
}

/**
 * 7. Napravi varijablu imaNalog = false.
 * Ako je imaNalog false, ispiši "Nema nalog".
 */

let hasAccount = false;
if (!hasAccount) {
    console.log('Nema nalog');
}

/**
 * 8. Napravi varijablu ulogovan = true.
 * Ako je ulogovan true, ispiši "Uspešno ste ulogovani".
 */

let isLogin = false;
if (isLogin) {
    console.log('Uspesno ste ulogovani');
}

/**
 * 9. Napravi varijablu punoletan = true.
 * Ako je punoletan true, ispiši "Dozvoljen pristup".
 */

let adult = true;
if (adult) {
    console.log('Dozvoljen pristup');
}

/**
 * 10. Napravi varijablu jeAdmin = false.
 * Ako je jeAdmin false, ispiši "Nemate admin prava".
 */

let isAdmin = false;
if (!isAdmin) {
    console.log('Nemate admin prava');
}